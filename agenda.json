---
layout: 
---

{"agendaList":[{% for item in site.data.agenda19 %}
	{
		"id"		: "{{ item.id }}",
		"day"		: "{{ item.day }}",
		"type"		: "{{ item.type }}",
		"time"		: "{{ item.time }}",
		"title" 	: "{{ item.title | strip_html }}",
		"author"	: "{{ item.author }}",
		"label"		: "{{ item.label }}",
		"place" 	: "{{ item.place }}"
	}{% unless forloop.last %},{% endunless %}
{% endfor %}
]}